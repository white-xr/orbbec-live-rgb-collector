# legacy_tools

这里保留的是旧采集入口和辅助后处理脚本，当前主流程不再直接使用。

当前推荐入口在项目根目录：

- GUI 启动器：`Run-Orbbec-RGB-Dataset-GUI.bat`
- 采集脚本：`scripts/`
- YAML 配置：`config/`
- 辅助检查工具：`tools/`

如果后面需要检查旧采集目录、生成视频，或者恢复旧 IR/双路脚本，可以从这里找回。
