# legacy_tools

这里放的是旧采集入口和辅助后处理脚本，当前主流程不再直接使用。

当前根目录只保留两组正式入口：

- RGB-D 采集：`Run-Orbbec-LiveCapture.bat` + `config.yaml`
- 双 RGB 采集：`Run-Orbbec-DualRGB-Capture.bat` + `config_dual_rgb.yaml`

如果后面需要检查旧采集目录、生成视频或恢复旧 IR/双路脚本，可以从这里找回。
